{
  "buildCommand": "mkdir -p vercel-output",
  "outputDirectory": "vercel-output",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ],
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  }
}
